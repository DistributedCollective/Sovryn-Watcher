/**
 * Monitor tester
 * 
 */

import conf from '../config/config';


describe('Monitor', async () => {
    describe('#Notifications', async () => {
        before(async () => {
            console.log("init");
            
        });
        
        it('should send a margin call email', async () => {
           
        });
    });
});